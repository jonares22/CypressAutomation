{
  "devDependencies": {
    "@cypress/grep": "^4.1.0",
    "allure-cypress": "^3.0.9",
    "cypress": "^13.17.0",
    "cypress-plugin-steps": "^1.1.1",
    "rimraf": "^6.0.1"
  },
  "scripts": {
    "cy:open": "cypress open",
    "allure:generate": "allure generate --clean --single-file reports/allure/allure-results -o reports/allure/allure-reports ",
    "allure:open": "allure open reports/allure/allure-reports",
    "allure:clean": "rimraf reports/allure/allure-results",
    "test:api": "npm run allure:clean && cypress run --env grepTags=@api",
    "test:ui": "npm run allure:clean && cypress run --env grepTags=@ui",
    "test:regression": "npm run allure:clean && cypress run --env grepTags=@regression"
  }
}
